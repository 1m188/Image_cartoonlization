[package]
name = "image-cartoonlization"
version = "1.0.0"
edition = "2021"
description = "Photo cartoonization CLI tool — ported from MATLAB to Rust"

[dependencies]
image = "0.25"
clap = { version = "4", features = ["derive"] }
rayon = "1.10"

[dev-dependencies]
tempfile = "3"
